# Research Inbox — 2026-09-11

> 内容由标题、元数据、摘要以及可获得时的公开正文方法章节生成。所有结论均为作者报告，未经独立验证。
> 数字可以来自标题、摘要或公开正文方法章节，并采用宽松的近似与四舍五入匹配。

## 1. MpSub: A Momentum $p$-Dimensional Subspace Trust-Region Method for Derivative-Free Fine-Tuning of Large Language Models

- 期刊/年份：arXiv (Cornell University)，2026
- 筛选分数：0.81
- DOI：[10.48550/arxiv.2609.07666](https://doi.org/10.48550/arxiv.2609.07666)
- 原始链接：https://doi.org/10.48550/arxiv.2609.07666
- 方法证据：摘要 + 公开正文方法章节

### 核心问题

针对大语言模型全参数微调中反向传播带来的高内存成本，以及现有零阶优化方法需要为每个模型和任务手动调整敏感学习率的问题，论文提出一种无需学习率调参的动量p维子空间信赖域方法（MpSub）。

### 方法概览与架构

MpSub是一种迭代零阶优化算法。每次迭代在当前参数点附近构造p维仿射子空间，其中第一维为最近接受步的归一化动量方向，其余p−1维为独立高斯随机方向；在子空间内通过中心差分估计方向导数形成子空间梯度，进而构建线性信赖域模型；通过求解信赖域子问题得到试探步，并根据预测与实际损失下降之比自适应更新信赖域半径。在LLM微调中，所有函数评估共享同一个小批量，随机方向由种子原位重建，仅用前向传播。

### 方法原理

MpSub的核心机制是用低维子空间探索代替高维全参数搜索，并用自适应信赖域半径代替人工学习率。高维高斯随机方向天然近似正交，无需显式正交化即可覆盖各向同性的参数空间；动量方向利用连续下降步间的方向相关性，将历史成功信息注入子空间而不增加函数评估。中心差分沿每个子空间方向估计方向导数，构建局部线性代理模型。信赖域半径Δ同时作为差分扰动尺度和代理模型有效范围：差分误差随Δ以线性界上界；线性模型最小化在信赖域约束下的闭式解给出沿子空间最速下降方向的步长，无需预设步长。预测下降与实际下降之比ρ用于解耦接受与半径调整：只要实测损失严格下降就接受候选点，避免浪费前向评估；半径在ρ≥η时扩张（模型可靠），ρ<η时收缩（非线性或误差主导），并施加安全上下界。因此，半径更新形成自动步长控制，使同一组超参数跨模型尺寸有效。在随机小批量设置中，迭代内共享小批量保证函数评估一致性，种子原地重建避免存储n维扰动，实现纯前向训练。

### 具体实施过程

**步骤 1**

MpSub算法维护参数迭代点x_k∈R^n、信赖域半径Δ_k∈[Δ_min,Δ_max]和动量位移m_k（最近接受步的参数变化）。每次迭代，优化限制在仿射子空间x_k+S_k内，其中S_k由p个方向向量张成。第一方向d_1由动量m_k归一化得到，其余p−1个方向独立从N(0,I_n)采样并乘以1/√n归一化期望模长；若无已接受步则全部方向随机采样。所有方向组装成帧矩阵D_k∈R^{n×p}，候选位移参数化为D_k s。

**步骤 2**

对于每个方向d_i，使用中心差分估计方向导数：g_i ≈ [f(x_k+Δ_k d_i) − f(x_k−Δ_k d_i)]/(2Δ_k)。这需要2p次函数评估。将所有估计堆叠成子空间梯度g_k∈R^p。信赖域半径Δ_k同时作为差分扰动尺度和信赖域界限，差分误差随Δ_k线性有界。

**步骤 3**

利用子空间梯度构建线性代理模型m(s)=f(x_k)+g_k^T s。在信赖域约束||s||≤Δ_k下最小化该线性模型：若g_k≠0，闭式解为s=−Δ_k g_k/||g_k||，即沿子空间最速下降方向到达边界；若g_k=0，则收缩半径并跳过试探点评估。映射回全参数空间得候选点x^+=x_k+D_k s。

**步骤 4**

评估候选点损失f^+后，计算预测下降与实际下降之比ρ_k=(f(x_k)−f^+)/pred_k。候选点接受与半径调整解耦：只要f^+<f(x_k)就接受x_{k+1}=x^+并设置动量m_{k+1}=x^+−x_k；由ρ_k决定半径更新：若ρ_k≥η则扩张Δ_k，若ρ_k<η则收缩Δ_k，并施加安全上下界。对于LLM微调，同一迭代内所有评估共享一个小批量，随机方向通过存储的种子原位重建而不显式存储，仅用前向评估。

**步骤 5**

相关定量细节未在标题、摘要或公开正文方法上下文中提供。两个模型使用完全相同的预设参数（包括p、信赖域初始半径和上下界），无需针对不同模型尺寸调整；而MeZO的学习率从网格中单独选择。训练完成后在测试集上评估准确率。

### 主要贡献

- 提出MpSub：一种动量p维子空间信赖域方法，用于大语言模型的无导数全参数微调，完全消除学习率超参数。
- 设计子空间构造：一维保留最近接受步的动量，其余p−1维通过独立高斯随机方向探索，实现历史方向与探索的平衡。
- 采用中心差分估计子空间梯度，并用线性信赖域模型计算试探步；信赖域半径根据预测与实测损失下降的吻合度自适应调整，同时控制差分扰动和步长。
- 在LLM微调中，同一迭代内共享小批量并通过存储种子原地重建方向，仅使用前向传播，避免存储参数级扰动向量。
- 针对光滑确定性目标，在未正交化高斯方向下给出有限差分误差界、子空间梯度能量捕获量化，并证明在安全半径更新下梯度范数几乎必然收敛到0。
- 相关定量细节未在标题、摘要或公开正文方法上下文中提供。

### 作者报告的结果

- 相关定量结果未在标题、摘要或公开正文方法上下文中提供。（证据：摘要）
- 两个模型尺寸使用完全相同的预设参数，无需学习率搜索。（证据：摘要）

### 与既有工作的区别

与现有零阶优化方法（如MeZO依赖预设学习率）及最接近的2D-MoSub（仅二维子空间且使用二次插值模型）相比，MpSub通过p维动量加探索子空间和线性信赖域模型，完全消除学习率超参数，并将每步函数评估成本从二次插值的O(p2)降至O(p)。

### 研究价值

该研究为大语言模型的全参数微调提供了一种内存高效、无需学习率调参的零阶优化方法，通过子空间探索和信赖域自适应机制，显著降低了超参数搜索成本，并在实际规模模型上验证了与调参基线相当的性能，有望推动大模型在资源受限环境下的高效适应。

### 局限与开放问题

- 理论分析假定目标函数为光滑确定性目标，而实验中使用随机小批量损失，理论与实践的差异未在摘要中完全弥合。
- 实验仅在OPT-125M和OPT-350M以及CommitmentBank数据集上进行，泛化到更大模型、其他任务或不同架构的证据未提供。
- 未提供对超参数p（子空间维度）、信赖域初始半径及上下界的敏感性分析。
- 未提到与其他先进零阶方法或低秩方法的系统性比较。
## 自动处理状态

- 本地结构与证据校验：`通过`
- 自动完成论文数：`1`
- 完成时间：`2026-09-11T01:00:59.624767Z`
- 人工评审：`不需要`
- 长期知识库：`待本次 workflow 更新`
- 每日汇总邮件：`待独立投递 workflow 发送`
